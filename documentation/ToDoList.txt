1. Нужно организовать компоненты карт таким образом, что б родительсий компонент
содержал в себе карты и параметры для их заполнения. 
что б можно было размещать карты группами и передовать в них значения из
родительского компонента в дочерние (карты)

git stash --include-untracked && git fetch --all && git reset --hard origin/master